<%= form_for(@score_by_hole) do |f| %>
  <% if @score_by_hole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score_by_hole.errors.count, "error") %> prohibited this score_by_hole from being saved:</h2>

      <ul>
      <% @score_by_hole.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
